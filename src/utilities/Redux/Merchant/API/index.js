const base_url = "https://dev.bill-indonesia.com/api/merchant"

const delete_api = `${base_url}/delete`

const merchant_list_api = `${base_url}/merchant-list`

const register_merchant_api = `${base_url}/register`